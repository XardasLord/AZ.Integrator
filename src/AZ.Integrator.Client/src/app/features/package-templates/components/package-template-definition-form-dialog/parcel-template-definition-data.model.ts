import { TagParcelTemplateViewModel } from '../../../../shared/graphql/graphql-integrator.schema';

export interface ParcelTemplateDefinitionDataModel {
  tag: string;
  template: TagParcelTemplateViewModel;
}
